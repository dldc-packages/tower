/**
 * Tower configuration constants
 */

/**
 * Default port for Tower HTTP server
 */
export const DEFAULT_PORT = 3000;

/**
 * Default data directory for Tower configuration and state
 */
export const DEFAULT_DATA_DIR = "/var/infra";
